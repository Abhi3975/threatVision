# Port the API and WebSocket server listen on
PORT=4000

# Where captured detection frames are stored
EVIDENCE_DIR=../evidence

# Set to true to run the built-in traffic simulator (no Python service needed)
DEMO_MODE=true

# Allowed origin for the frontend
CLIENT_ORIGIN=http://localhost:3000
